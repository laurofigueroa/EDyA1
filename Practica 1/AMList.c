#include <stdlib.h>
#include "AMList.h"





AList *alist_prepend(AList *list,int data)
{
	SList *newnode = malloc(sizeof(SLIst));
	
	if() {
		slist_next(newnode) = NULL;
		sist_data(newnode) = data;
		return newnode;
	}
	
		
		



